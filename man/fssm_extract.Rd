% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fssm_extract.R
\name{fssm_extract}
\alias{fssm_extract}
\title{Master FSSM Data Extractor}
\usage{
fssm_extract(
  input,
  syntax = NULL,
  scales = NULL,
  data = NULL,
  target_construct = NULL,
  treatment_construct = NULL,
  indicator_weighting = c("entropy", "equal")
)
}
\arguments{
\item{input}{Either a \code{boot_seminr_model} OR a \code{data.frame}.}

\item{syntax}{(Required for Raw Data) A string defining constructs (e.g., "IM =~ item1 + item2").}

\item{scales}{Optional. A named list of min/max scales (e.g., list(PU_01=c(1,5))).
For SEM: Required for 0-100 normalization.
For Raw: If provided, uses theoretical rescaling; if NULL, uses observed min/max.}

\item{data}{(Optional for SEM) Raw indicator data. If NULL, extracted from model.}

\item{target_construct}{String. Name of the outcome variable (Y).}

\item{treatment_construct}{String. Name of the treatment variable (X).}

\item{indicator_weighting}{String. How to weight indicators for raw data route:
\itemize{
\item "entropy" (default): Shannon entropy weights (more discriminating items get higher weight)
\item "equal": Equal weights (simple mean of items)
}}
}
\value{
An object of class \code{fssm_object}.
}
\description{
Prepares a unified FSSM object from either a PLS-SEM model or raw data.
Implements Step 1 of the FSSM workflow:
\itemize{
\item PLS-SEM route: Uses outer weights + theoretical min/max rescaling (cIPMA-identical)
\item Raw data route: Uses entropy/equal weighting + observed or theoretical rescaling
}
}
