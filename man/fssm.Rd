% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fssm.R
\name{fssm}
\alias{fssm}
\title{Run Fuzzy Subjective Structure Model (FSSM) Analysis}
\usage{
fssm(
  input,
  target_construct,
  data = NULL,
  scales = NULL,
  target_level = 85,
  confounders = NULL,
  nca_rep = 10000,
  weighting = c("gps", "binary"),
  alpha_threshold = 0.85,
  epsilon_threshold = 0.05,
  delta_threshold = 0.15,
  beta_threshold = 0.2,
  pls_model = NULL
)
}
\arguments{
\item{input}{Either a bootstrapped seminr model OR an object from \code{fssm_extract()}.}

\item{target_construct}{String. The name of the dependent variable.}

\item{data}{Optional. Raw data frame (required if input is a model).}

\item{scales}{Optional. List of scales (required if input is a model).}

\item{target_level}{Numeric. The desired level of the target construct (0-100 scale). Default is 85.}

\item{confounders}{Character vector. Names of confounding variables for GPS weighting.}

\item{nca_rep}{Integer. Number of permutations for NCA. Default 10000.}

\item{weighting}{Character. Either "gps" (default) or "binary" for IPW method.}

\item{alpha_threshold}{Numeric. Threshold for typical sufficiency. Default 0.85.}

\item{epsilon_threshold}{Numeric. Threshold for typical necessity exception rate. Default 0.05.}

\item{delta_threshold}{Numeric. Threshold for probabilistic sufficiency uplift. Default 0.15.}

\item{beta_threshold}{Numeric. Threshold for probabilistic necessity. Default 0.20.}

\item{pls_model}{Optional. The base (non-bootstrapped) seminr model for full diagnostics.}
}
\value{
An object of class \code{fssm} containing results, claims, weights, and diagnostics.
}
\description{
The main function to perform FSSM analysis. It accepts either a raw dataset (via fssm_extract)
or a PLS-SEM model. It orchestrates the NCA bottleneck calculation, fuzzy membership estimation,
weighting (GPS or Binary), and causal claim validation.

IMPORTANT: The pre-FSSM baseline (Importance, Performance, NCA_d, Bottleneck levels)
is computed IDENTICALLY to cIPMA. FSSM-specific steps (fuzzy membership, GPS/binary weighting,
causal claims) are applied on top of this baseline.
}
